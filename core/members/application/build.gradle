dependencies {
    implementation(project(':commons:spring:aop'))
    implementation(project(':core:members:domain'))

    implementation 'org.springframework:spring-tx'
    implementation 'org.springframework.boot:spring-boot-starter-cache'
}